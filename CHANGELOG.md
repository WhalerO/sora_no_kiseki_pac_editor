# Changelog

本项目采用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的组织
方式；正式版本计划遵循语义化版本。当前开发版本为 `0.1.3.dev20260917`。

## [Unreleased]

### Fixed

- 一键构建保持当前 PowerShell 版本，按脚本位置定位项目；虚拟环境、依赖安装或打包失败时立即停止，不再误报完成。
- 显式按 UTF-8 读取打包自检及 VLC 清单，修复 Windows PowerShell 5.1 在中文路径下错误解码 JSON 的问题。
- 构建前清理上次的完成标记及自检结果，避免新 EXE 与旧构建信息混用；补充 PowerShell 5.1/7 的入口与错误传播回归测试。

## [0.1.3.dev20260917] - 2026-09-17

### Fixed

- Windows 工作区锁改用不覆盖目标的重命名发布，不再依赖硬链接，修复部分存放环境打开 PAC 时的 WinError 1。
- 用 Windows 进程间互斥保护锁发布、失效锁回收及释放，避免并发回收误删新锁；不覆盖活跃锁或删除归属不明的锁。
- 缩短会话目录和临时锁文件名，降低深层路径触发 Windows 长路径限制的风险；不改变缓存的默认位置。
- 缓存锁操作失败时明确说明数据目录、系统原因与可用处理方法，避免被误认为游戏 PAC 损坏。
- 增加硬链接不可用、并发竞争、异常退出恢复、写入失败及短路径生成的回归测试。

## [0.1.2.dev20260916] - 2026-09-16

### Added

- 项目所有者选择 MIT 作为自有代码的许可证，第三方组件保留原许可证。
- 发布材料保留第三方组件致谢与许可证说明。
- 附带 `常用文本替换_不含金.json` 作为可导入的参考方案，共 30 条规则；不包含“金”的替换和章节图片。

### Fixed

- 正文搜索支持后台逐步显示结果，优化大批量文本扫描时的界面响应。
- 优化 DAT 指针扫描修复流程与差异文本展示。
- 修正 PAC 导出后的状态判断，区分相对原包已修改与尚未导出的修改。

## [0.1.1.dev20260916] - 2026-09-16

### Fixed

- PNG/DDS、模型纹理和字体图集共用只读 LZ4 解压入口，验证帧完整性与解压大小；原资源保持不变。
- Windows exe 嵌入 Per-Monitor V2 DPI 声明，正确处理 API 失败，并随显示器 DPI 变化重算字体和布局。
- 冻结版缓存默认迁至 exe 旁的 `TIS_Retext_Data`，不再默认占用系统盘；包内 probe 临时文件使用受管缓存。
- 冻结包 probe 覆盖 LZ4 DDS 解码并记录 DPI 感知状态。

## [0.1.0.dev20260916] - 2026-09-16

本次计划发布 Windows x64 编译预览包，新版源码暂不发布。根项目许可证待定；
未声明对尚未发售的 the 2nd 正式版完成兼容性验证。

### Fixed

- 默认烟测改用现场生成的原创合成 TBL/DAT，覆盖四条引擎/格式组合的变长保存与全文回读，不再依赖私有样本。
- 冻结包 probe 增加四条文本引擎/格式组合的变长回读和合成 PAC 提取验证。

### Added

- PAC 文件/目录/多选提取、非文本文件替换、批量插入外部文件和资源树右键菜单；
- 编辑器使用指南与当前游戏 PAC 的只读源文件集成检查脚本；
- FPAC v1 严格解析、懒物化工作区、多 PAC 文件树和流式重建；
- PAC/PAC 组与解包目录的独立版本对比；
- PNG/DDS、WAV、WebM、FNT、MI 和 MDL 只读预览；
- MDL 外部贴图、名称追溯、GPU/CPU 渲染及兼容骨骼/刚性节点动画；
- 预览条目提取与另存为；
- 固定 VLC runtime 准备、打包后 native probe、版本资源和 build manifest；
- 第三方组件清单与许可证收集基础设施。
- 批量映射支持逐对设置“完全匹配”，并通过映射 JSON v2 保存每一行的匹配方式；

### Changed

- 字体和行高按实际 DPI/字体度量设置，侧栏跟随缩放；高级选项移入设置窗口；
- 简化单文件和批量页，固定保留底部状态栏及映射编辑操作，刷新文件树保留展开位置；
- 单文件保存进一步隔离到副本暂存：验证失败不发布文件，也不改变正在编辑的文档；
- PAC 名称读取从逐字节循环改为有界块读取，多文件插入只发布一次工作区清单；
- 新增文件支持撤销后重建、空文件和正确的缓存编号重建，导入失败不会发布部分清单；
- TBL游戏版本选择改为“逐文件已证实布局优先、全局版本仅作未知布局后备”，
  修复手动选择2nd时漏读 `t_quest_fc.tbl`、`t_books.tbl` 等 FC/Sora1 布局文本；
- 批量替换统一收敛到结构化 TBL/DAT 服务；旧版启发式 RAW/REPACK 批处理已禁用；
- 冻结版程序数据从 exe 旁迁移到 `%LOCALAPPDATA%\TIS_Retext`；
- 三维模型解析、动画、关联与渲染从 GUI 主功能中解耦；
- Python 运行依赖增加兼容上界，严格 Windows 构建固定到已验证版本和
  CPython 3.11；
- PyInstaller 资源收集缩小到运行所需 Kuro schema、脚本和许可证。
- 独立 FFmpeg/imageio-ffmpeg 已从默认依赖和发行包移除；WebM 元数据改用有界
  EBML 解析，首帧与拖动定位统一复用 LibVLC；
- 构建临时目录固定到项目自身的 `.runtime/`，严格 Release 拒绝 Conda，并排除
  VLC GUI 插件和重复的 `libvlccore.dll`；
- 固定 VLC 运行时增加逐文件 SHA-256、自包含来源检查和实际 PCM 解码 probe，
  发行目录显式拒绝独立 FFmpeg。

### Security

- 批量目标无论由何种父节点展开，都拒绝 TBL/DAT 之外的文件；
- PAC 构建、工作区物化和批量保存增加路径、指纹、暂存、回读及外部变化检查；
- 发行构建清理旧输出，并拒绝混入运行数据、PAC、MDL 或 DDS；
- 严格 Release 门禁要求解决根许可证、真实语料授权和 VLC 对应源码审核问题。

### Known release blockers

- 根项目许可证尚未由项目所有者选择；
- 真实 corpus 及部分样本的再分发授权尚未完成审计；
- 固定 VLC runtime 的完整组件通知与对应源码条件仍需在正式发布前复核；
- 因此当前仅为开发版本，不应公开分发构建产物。
